{
  "data": {
    "contractNumber": 3,
    "items": [
      {
        "additionalClassifications": [
          {
            "description": "{{ contracts[5]['items'][0].additionalClassifications[0].description }}",
            "id": "{{ contracts[5]['items'][0].additionalClassifications[0].id }}",
            "scheme": "{{ contracts[5]['items'][0].additionalClassifications[0].scheme }}"
          }
        ],
        "classification": {
          "description": "{{ contracts[5]['items'][0].classification.description }}",
          "id": "{{ contracts[5]['items'][0].classification.id }}",
          "scheme": "{{ contracts[5]['items'][0].classification.scheme }}"
        },
        "deliveryAddress": {
          "countryName": "{{ contracts[5]['items'][0].deliveryAddress.countryName }}",
          "locality": "{{ contracts[5]['items'][0].deliveryAddress.locality }}",
          "postalCode": "{{ contracts[5]['items'][0].deliveryAddress.postalCode }}",
          "region": "{{ contracts[5]['items'][0].deliveryAddress.region }}",
          "streetAddress": "{{ contracts[5]['items'][0].deliveryAddress.streetAddress }}"
        },
        "deliveryDate": {
          "endDate": "{{ contracts[5]['items'][0].deliveryDate.endDate }}",
          "startDate": "{{ contracts[5]['items'][0].deliveryDate.startDate }}"
        },
        "description": "{{ contracts[5]['items'][0].description }}",
        "description_en": "{{ contracts[5]['items'][0].description_en }}",
        "id": "{{ contracts[5]['items'][0].id }}",
        "quantity": 1,
        "relatedBuyer": "{{ contracts[5]['items'][0].relatedBuyer }}",
        "relatedLot": "{{ contracts[5]['items'][0].relatedLot }}",
        "unit": {
          "code": "{{ contracts[5]['items'][0].unit.code }}",
          "name": "{{ contracts[5]['items'][0].unit.name }}",
          "value": {
            "amount": 10.0,
            "valueAddedTaxIncluded": false
          }
        }
      }
    ],
    "period": {
      "endDate": "{{ from_now_iso(days=365) }}",
      "startDate": "{{ from_now_iso() }}"
    },
    "status": "active",
    "value": {
      "amount": 100,
      "amountNet": 100,
      "valueAddedTaxIncluded": false
    }
  }
}